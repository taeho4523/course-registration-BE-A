spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
    show-sql: true
    open-in-view: false

enrollment:
  cancel:
    period-days: 7
  waitlist:
    payment-deadline-hours: 24
